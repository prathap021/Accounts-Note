# Copy patterns:
#   Publishable only → .env
#   Secrets → .secret.local (emulator) + firebase functions:secrets:set (prod)

STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Do NOT put these in .env (conflicts with Secret Manager on deploy):
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx
